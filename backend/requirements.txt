# InvoiceFi Backend Dependencies
# Install: pip install -r requirements.txt
#
# Core Django stack
Django>=5.2,<6.0
djangorestframework>=3.16,<4.0
django-cors-headers>=4.7,<5.0
python-dotenv>=1.0,<2.0

# JWT validation (Supabase token verification)
PyJWT>=2.10,<3.0
cryptography>=44,<47

# HTTP client (Supabase profile API calls)
requests>=2.32,<3.0

# Blockchain (Polygon Amoy on-chain verification)
# web3 pulls in its own eth-* dependencies automatically
web3>=7.0,<8.0

# Deployment additions
gunicorn==26.0.0
psycopg2-binary==2.9.12
dj-database-url==3.1.2
whitenoise==6.12.0
